# HOW TO GET STARTED

1. Install node_modules in both server and client.
2. Clone the .env.example as .env, and add the values to it
3. Setup your DB
4. Build both the server and client code (using npm run build).
5. Run the server (using npm run start)
6. Open the html file to use
